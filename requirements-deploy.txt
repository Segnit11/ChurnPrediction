# Slim runtime dependencies for the deployed backend (Hugging Face Space).
# Versions pinned to match the artifacts the models were trained/pickled with
# (scikit-learn 1.6.1). google-generativeai is intentionally omitted — the app
# falls back to rule-based explanations when it's absent (see src/utils.py).
Flask==3.1.0
flask-cors==5.0.1
gunicorn==23.0.0
pandas==2.2.3
numpy==2.2.4
scipy==1.15.2
scikit-learn==1.6.1
imbalanced-learn==0.13.0
sklearn-compat==0.1.3
joblib==1.4.2
xgboost==3.0.0
gdown==5.2.0
